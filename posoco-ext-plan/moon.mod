name = "colmugx/posoco-ext-plan"

version = "0.1.0"

import {
  "colmugx/posoco@0.10.1",
  "moonbitlang/async@0.20.5",
}

readme = "README.mbt.md"

license = "Apache-2.0"

keywords = [ "posoco", "plan", "plan-mode", "read-only", "ai-agent" ]

description = "Plan mode extension for Posoco: read-only tool gating, plan prompt injection, and an exit-plan approval flow"

source = "src"
