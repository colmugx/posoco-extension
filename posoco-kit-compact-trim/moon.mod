name = "colmugx/posoco-kit-compact-trim"

version = "0.1.0"

import {
  "colmugx/posoco@0.14.3",
}

license = "Apache-2.0"

keywords = [ "posoco", "compact", "trim", "fallback", "context-window" ]

description = "Posoco deterministic context trim kit — LLM-free whole-round transcript trim used as the compact fallback for overflow recovery"

source = "src"
