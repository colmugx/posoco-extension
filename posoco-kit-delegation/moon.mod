name = "colmugx/posoco-kit-delegation"

version = "0.1.0"

import {
  "colmugx/posoco@0.14.3",
}

license = "Apache-2.0"

keywords = [ "posoco", "delegation", "intent", "phase", "kit" ]

description = "Posoco delegation kit — provider-neutral delegation intent, tool call and phase types shared by the host kits"

source = "src"
