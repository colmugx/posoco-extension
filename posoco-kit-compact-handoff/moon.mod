name = "colmugx/posoco-kit-compact-handoff"

version = "0.1.0"

import {
  "colmugx/posoco@0.14.3",
}

license = "Apache-2.0"

keywords = [ "posoco", "compact", "handoff", "new-thread", "session" ]

description = "Posoco NewThread handoff kit — summary into a fresh session plus a searchable pointer back to the persisted old-session transcript"

source = "src"
