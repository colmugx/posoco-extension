name = "colmugx/posoco-kit-compact-evict"

version = "0.1.0"

import {
  "colmugx/posoco@0.14.3",
}

license = "Apache-2.0"

keywords = [ "posoco", "compact", "evict", "tool-result", "auto-compact" ]

description = "Posoco compact-evict kit — turn-protected tool-result eviction for Auto compaction: removes whole tool-call/result pairs older than the most recent user turns while keeping the conversation skeleton (system, user, assistant text) verbatim"

source = "src"
