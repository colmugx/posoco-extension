name = "colmugx/posoco-kit-chat-completions"

version = "0.1.0"

import {
  "colmugx/posoco@0.9.0",
}

license = "Apache-2.0"

keywords = [ "posoco", "sse", "streaming", "chat-completions", "model-port" ]

description = "Posoco chat-completions kit — shared OpenAI chat-completions dialect parsing (SSE streaming) with a uniform real-world tolerance policy"

source = "src"
