name = "colmugx/posoco-kit-responses"

version = "0.1.0"

import {
  "colmugx/posoco@0.15.0",
}

license = "Apache-2.0"

keywords = [ "posoco", "sse", "streaming", "responses", "model-port" ]

description = "Posoco responses kit — shared OpenAI Responses dialect parsing (input items, tool calls, usage, reasoning replay, SSE streaming) with a uniform real-world tolerance policy"

source = "src"
