name = "colmugx/posoco-ext-herdr"

version = "0.1.0"

import {
  "colmugx/posoco@0.11.1",
  "moonbitlang/async@0.21.0",
}

readme = "README.md"

license = "Apache-2.0"

keywords = [ "posoco", "herdr", "observer", "agent-presence", "terminal" ]

description = "Herdr presence extension for Posoco: reports agent lifecycle state to the herdr terminal multiplexer via a single Observer port"

source = "src"
