import {
  "moonbitlang/core/debug",
  "moonbitlang/core/env",
  "moonbitlang/x/path",
}

import {
  "moonbitlang/async",
} for "wbtest"

options(
  targets: { "metrics_wbtest.mbt": [ "native" ] },
)
