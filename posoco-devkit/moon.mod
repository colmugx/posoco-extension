name = "colmugx/posoco-devkit"

version = "0.1.0"

readme = "README.mbt.md"

license = "Apache-2.0"

keywords = [ "posoco", "devkit", "extension", "logger" ]

description = "Small helper layer for Posoco extension authors"

source = "src"
